name: format
on:
  pull_request:
    branches: [master]
    paths:
    - '**/*.kts'
    - '**/*.kt'

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout repository
        uses: actions/checkout@v5
      - name: setup jdk
        uses: actions/setup-java@de5a937a1dc73fbc1a67d7d1aa4bebc1082f3190 # v5.0.0+5
        with:
          java-version: '25'
          distribution: 'microsoft'
      - name: setup gradle
        uses: gradle/actions/setup-gradle@v5
      - name: format
        run: ./gradlew ktfmtFormat
      - name: suggester / ktfmt
        uses: reviewdog/action-suggester@v1
        with:
          tool_name: ktfmt
